# Gradle
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
# Loom is not compatible with the configuration cache yet.
org.gradle.configuration-cache=false

# Fabric — verified against https://fabricmc.net/develop
minecraft_version=26.1.2
loader_version=0.19.3
loom_version=1.17.19

# KoHs OptiMatch
mod_id=kohs_optimatch
mod_name=KoHs OptiMatch
mod_version=0.1.0
maven_group=dev.zymekoh

# Dependencies
fabric_api_version=0.155.2+26.1.2
modmenu_version=18.0.0
# Matches the ASM bundled by Fabric Loader 0.19.3 (compile-only: the loader provides it at runtime)
asm_version=9.10.1
